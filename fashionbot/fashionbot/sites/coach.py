"""Coach and Coachtopia source extraction.

The first implementation uses the generic classifier and keeps ambiguous page
states as manual checks.
"""

from fashionbot.sites.generic import classify_signals
