"""Topologie source extraction.

The first implementation uses the generic classifier because the exact product
pages expose direct purchase/sold-out text.
"""

from fashionbot.sites.generic import classify_signals
